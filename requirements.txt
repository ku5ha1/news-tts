# FastAPI and ASGI server
fastapi==0.110.0
uvicorn[standard]==0.29.0

# Database
motor==3.4.0
pymongo==4.6.0

# Data validation and serialization
pydantic==2.6.3
pydantic-settings==2.1.0

# Authentication
PyJWT==2.8.0
bcrypt==4.1.2

# HTTP and async support
httpx==0.25.2
aiofiles==23.2.1
python-multipart==0.0.6

# PyTorch with CUDA support (falls back to CPU if CUDA unavailable)
# Install from PyPI with CUDA 12.4 support for NVIDIA GPUs
# Note: --extra-index-url adds to default PyPI, not replaces it
--extra-index-url https://download.pytorch.org/whl/cu124
torch>=2.5.0
torchvision>=0.20.0
torchaudio>=2.5.0

# Translation - IndicTransToolkit requirements
numpy>=2.1.0
transformers>=4.51.0    
huggingface_hub>=0.15.0
sentencepiece==0.2.0
sacremoses==0.1.1
protobuf==4.25.3
indictranstoolkit>=1.1.0

# ElevenLabs TTS (deprecated - using Azure Speech instead)
# elevenlabs==1.3.0

# Azure Blob Storage
azure-storage-blob==12.19.0

# Azure Document Intelligence (for OCR)
azure-ai-documentintelligence

# Azure Speech (for TTS)
azure-cognitiveservices-speech

# Qdrant Cloud and FastEmbed (for local embeddings)
qdrant-client>=1.7.0
fastembed>=0.2.0

# Language detection
langdetect==1.0.9

# Environment and configuration
python-dotenv==1.0.1

# Utilities
python-dateutil==2.8.2

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1

# Logging and monitoring
structlog==23.2.0
colorama==0.4.6